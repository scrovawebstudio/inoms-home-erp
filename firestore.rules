rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // INOMS Architecture: All business data is authoritative on Home Server SQLite.
    // Firestore does not store business collections.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
